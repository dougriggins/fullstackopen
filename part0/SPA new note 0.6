title part0 0.6 SPA new note diagram

note over browser:
browser uses JS code in spa.js to process form and add
new note to the notes list, then rerenders the page and sends
the note to the server
content-type of the POST request is set to application/json
end note

browser->server:HTTP:POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
server->browser:STATUS-CODE 201 (created)

note over server:
server does not redirect and just sends the code for "created"
end note